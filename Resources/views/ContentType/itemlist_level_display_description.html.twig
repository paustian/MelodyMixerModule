{# purpose of this template: Display levels within an external context #}
{% trans_default_domain 'level' %}
<dl>
    {% for level in items %}
        <dt>{{ level|paustianmelodymixermodule_formattedTitle }}</dt>
        {% if level.levelName %}
            <dd>{{ level.levelName|striptags|u.truncate(200, '…') }}</dd>
        {% endif %}
    {% else %}
        <dt>{% trans %}No levels found.{% endtrans %}</dt>
    {% endfor %}
</dl>
